build/
*.bin
*.elf
*.map
.DS_Store
firmware/AirRadar/build/
node_modules/

# ...but the released flasher images ARE the deliverable — track them.
!flasher/airradar-merged.bin
!flasher/airradar-ota.bin

# Tooling scratch
.playwright-mcp/
.DS_Store

# Internal design artifacts — kept locally, deliberately not published.
# The UI/UX audit is candid internal criticism and the mockups are working
# process, neither of which belongs in a public repo.
UI_UX_REVIEW.md
docs/mockups/
